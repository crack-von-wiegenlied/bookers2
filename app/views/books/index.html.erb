<h1>Books#index</h1>
<p>Find me in app/views/books/index.html.erb</p>



<div>
  <h4>New book</h4>
  <%= form_with model: @book do |f| %>
    <label>Title</label>
      <%= f.text_field :title %>
    <lavel>Opinion</lavel>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>

  <% @books.each do |book| %>
  <table>
    <thread>
      <tr>
        <tr><%= book.title %></tr>
        <tr><%= book.body %></tr>
      </tr>
    </thread>
  </table>
  <% end %>



</div>
